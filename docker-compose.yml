version: '3.8'

services:
  webapp:
    build: .
    container_name: loginwebapp
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=database-1.cevyoqyq8e62.us-east-1.rds.amazonaws.com
      - DB_USER=admin
      - DB_PASS=admin12345
    depends_on:
      - db

   
   